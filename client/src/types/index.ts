export interface ImageSlider{
  imageUrl: string;
  url: string;
}

export interface Product{
  id: number;
  name: string;
  price: number;
  imageUrl: string;
  discount: number;
  rating: number;
}